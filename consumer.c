#include "buffer.h"
#include <iostream>

int main() {
  int nextConsumed;
  while (true) {
    while (count == 0)
      ;
    next_consumed = buffer[out];
    out = (out + 1) % BUFFER_SIZE;
    count--;
  }
}
